# 아키텍처 검토서 (2026-07-15) — 요약본

> **원문은 압축됐다.** 이 검토서가 제기한 11건은 **전부 02 문서에 반영되어 종결**됐고,
> 그 뒤 실제 구현까지 끝났다. 그래서 논증 과정을 남겨 둘 이유가 없어졌고, 아래
> **"무엇을 지적했고 어디서 해결됐는가"** 추적표만 남긴다.
> 전문은 git 이력에 있다 — `git log -p -- "Doc/설계/00_아키텍처_검토서_260714.md"`

**대상**: AI 자동 게임 생성 플랫폼 (Web + 오케스트레이터 + Strategic/Developer/QA AI)
**결론 당시 판정**: 3-Agent 오케스트레이션 구조 자체는 검증된 패턴이며 방향은 맞다.
다만 아래 11건이 도면에 빠져 있어 그대로 착수하면 초기 스프린트에서 재설계가 난다.

---

## 지적 사항 11건과 종결 위치

| # | 지적 | 어디서 해결됐나 |
|---|---|---|
| 3.1 | `AI Game Platform Backend` 와 `DeveloperAI` 의 관계가 모순 | 02 §1 「핵심 정정 사항」 — 둘은 같은 것이고 나머지는 전부 그 아래 MCP 서버 |
| 3.2 | Git 배포 파이프라인이 도면에 없음 | 02 §2.6 `GitMcpServer`, §3 `Deployment` 노드 |
| 3.3 | QA↔Developer 피드백 루프에 종료 조건 없음 | 02 §3·§7.2 (상한 초과 시 `HumanEscalation`). 실제 강제는 `app/graph/graph.py` 의 조건부 엣지 |
| 3.4 | Unity Editor 동시성 병목 (최대 실무 리스크) | 02 §7.1 — Job Queue 직렬화가 MVP 방침 |
| 3.5 | 사람 승인 게이트 부재 | 02 §3 `ApprovalGate`. 이후 청사진 **이전**에 도는 `ConceptGate` 가 추가돼 더 앞당겨짐 (03 §3.1) |
| 3.6 | 에셋 생성 주체 불명확 | 02 §2.5 `AssetGenMcpServer` 신설 |
| 3.7 | Job 상태 관리·실시간 통신 설계 부재 | 02 §5.5 `JobState`, §6 SSE 엔드포인트 |
| 3.8 | 에이전트 간 데이터 계약(JSON Schema) 없음 | 02 §5 전체, 그리고 03 `MCP_Interface_Specification` 이 도구 단위 계약으로 확장 |
| 3.9 | 에러/재시도/타임아웃 정책 부재 | 02 §7.2 |
| 3.10 | 보안 — Git 자격증명, 생성 코드 샌드박싱 | 02 §7.3, §2.4 「보안 정책」 |
| 3.11 | 모델 선택 전략 미구체화 | 02 §7.5 |

## 당시 인정한 강점 (설계 방향으로 계속 유지 중)

- 기획/구현/검증을 **별도 LLM+MCP 서버로 분리** — 역할마다 모델을 따로 고를 수 있다
- Unity 조작을 `UnityMcpServer` + `UnityEditorPlugin` 으로 나눠 **도구 제공과 실제 실행을 분리**
- QA 를 제3자 AI 로 분리해 **self-check bias** 를 줄임
- 각 단계 산출물이 다음 단계의 입력이 되는 **파이프라인 사고**

---

→ 이 검토를 반영한 설계 명세서: [02_시스템_설계명세서.md](02_시스템_설계명세서.md)
