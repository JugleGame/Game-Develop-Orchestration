mcp>=1.28.0
asyncpg>=0.30.0
anthropic>=0.60.0          # 기획 생성용 (research_idea 는 없어도 동작)
# 선택: 의미 검색. 없으면 트라이그램으로 자동 강등된다.
# 카드 임베딩과 반드시 같은 모델이어야 한다 (jhgan/ko-sroberta-multitask, 768차원).
# sentence-transformers>=3.0.0
